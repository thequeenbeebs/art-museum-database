

<%= form_for @painting do |f| %>
    <% if @painting.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(@painting.errors.count, "error") %> prohibited this article from being saved: </h2>

        <ul>
        <% @painting.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
    <% end %> 
    
    <p>
        Name:<br><%= f.text_field :name %>
    </p>

    <p>
        Year:<br><%= f.text_field :year %><br>
    </p>

    <p>
        Image URL:<br><%= f.text_field :img_url %><br>
    </p>

    <p>
        Artist:<br> <%= f.text_field :artist_name %><br>
    </p>

    <p>
        Museum:<br> <%= f.text_field :museum_name %><br>
    </p>

    <%= f.submit %>
<% end %>